# 项目规则 (Project Rules)

## 总体原则

本项目遵循 Superpowers 开发方法论，所有开发工作必须按照以下规则执行。

## 1. 工作流程规则

### 1.1 构思优先 (Brainstorm First)
- 在编写任何代码之前，先进行充分的设计讨论
- 明确功能需求、用户场景、技术方案
- 探索多种可能的实现方式
- 记录设计决策过程

### 1.2 详细计划 (Detailed Planning)
- 批准设计后，创建详细的实施计划
- 将工作拆分为独立、小的任务
- 每个任务都要包含：
  - 确切的文件路径
  - 完整的代码实现
  - 验证步骤
  - 预期 2-5 分钟完成

### 1.3 测试驱动 (Test-Driven)
- **始终**先写测试，再写代码
- 遵循红-绿-重构流程：
  1. 写一个失败的测试（红）
  2. 写最少的代码让测试通过（绿）
  3. 重构代码保持简洁（重构）
- 永远不要先写代码再补测试

### 1.4 子代理开发 (Subagent-Driven)
- 每个任务使用独立的子代理执行
- 提供清晰、明确的任务说明
- 每个任务完成后进行审查
- 只有前一个任务通过才继续下一个

### 1.5 代码审查 (Code Review)
- 每个任务完成后必须审查
- 对照计划检查输出
- 按严重程度报告问题：
  - 🔴 严重：阻止继续，必须修复
  - 🟡 中等：建议修复
  - 🟢 轻微：可选改进

## 2. 代码质量规则

### 2.1 TypeScript 使用
- 启用 TypeScript 严格模式
- 为所有函数和变量定义类型
- 使用接口定义数据结构
- 避免使用 `any` 类型

### 2.2 代码风格
- 使用 2 空格缩进
- 使用单引号
- 使用分号
- 函数和变量用 camelCase
- 类和接口用 PascalCase
- 常量用 UPPER_SNAKE_CASE

### 2.3 错误处理
- 始终处理可能的错误
- 使用 try/catch 包裹异步操作
- 提供有意义的错误信息
- 使用 Notice 向用户显示友好提示

### 2.4 代码组织
```
src/
├── main.ts                 # 插件入口
├── api/
│   └── ima-api.ts          # IMA API 客户端
├── ui/
│   ├── modals/             # 模态框组件
│   └── settings/           # 设置组件
├── utils/
│   ├── file-utils.ts       # 文件工具
│   └── string-utils.ts     # 字符串工具
└── types/
    └── index.ts            # 类型定义
```

## 3. Git 规则

### 3.1 分支管理
- `main` 分支保持稳定可发布
- 新功能用 `feature/` 前缀
- 修复用 `fix/` 前缀
- 重构用 `refactor/` 前缀

### 3.2 提交信息
```
<type>(<scope>): <subject>

<body>
```

类型（type）：
- `feat`: 新功能
- `fix`: 修复
- `docs`: 文档
- `style`: 格式
- `refactor`: 重构
- `test`: 测试
- `chore`: 构建/工具

### 3.3 提交前检查
- [ ] 代码可以正常构建
- [ ] 所有测试通过
- [ ] 没有调试语句 (console.log)
- [ ] 提交信息符合规范

## 4. 验证规则

### 4.1 功能验证
- 每个功能都要有对应的测试
- 手动验证用户场景
- 检查边界条件
- 验证错误处理

### 4.2 兼容性验证
- 确保与最低 Obsidian 版本兼容
- 测试不同平台 (Windows/macOS/Linux)
- 验证大文件处理

## 5. 文档规则

### 5.1 代码注释
- 为公共 API 添加 JSDoc 注释
- 复杂逻辑添加说明注释
- 更新 README 文档

### 5.2 变更记录
- 重大变更记录在 CHANGELOG.md
- 保持文档与代码同步

## 6. 开发命令

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建
npm run build

# 类型检查
tsc -noEmit
```

## 7. 紧急例外

只有在以下情况可以跳过部分流程：
- 生产环境严重问题需要快速修复
- 有明确的负责人批准
- 事后补全流程和文档

---

**记住**: 这些规则是为了提高代码质量和开发效率，不是为了束缚。如果有更好的方法，随时讨论改进！
