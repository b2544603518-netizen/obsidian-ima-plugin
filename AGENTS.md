# AGENTS.md

## 项目概述

这是一个 Obsidian 插件，用于同步 IMA 知识库到 Obsidian。

## 技术栈

- TypeScript
- Obsidian API
- esbuild

## 核心流程

### 1. 构思阶段 (Brainstorming)

在开始编写代码之前，先进行充分的设计讨论：

- 明确要解决的问题
- 探索多种解决方案
- 评估技术选型的利弊
- 记录设计决策过程

### 2. 规范阶段 (Spec Writing)

编写清晰的功能规范：

- 描述功能的输入输出
- 定义用户界面设计
- 列出边缘情况和错误处理
- 将规范分解为可读的部分供审核

### 3. 计划阶段 (Plan Writing)

创建详细的实施计划：

- 将工作拆分为独立任务
- 每个任务包含完整的文件路径
- 每个任务提供完整代码
- 包含验证步骤
- 确保每个任务可在 2-5 分钟内完成

### 4. 实施阶段 (Subagent-Driven Development)

使用子代理进行开发：

- 为每个任务启动新的子代理
- 提供明确的任务说明
- 审查每个任务的输出
- 只有在前一个任务完成后才继续下一个

### 5. 测试驱动开发 (TDD)

遵循严格的 TDD 流程：

- 首先编写失败的测试
- 验证测试确实失败
- 然后编写最简单的代码使测试通过
- 重构代码保持简洁
- 永远不要在测试之前编写代码

### 6. 代码审查 (Code Review)

每个任务完成后进行审查：

- 对照计划检查输出
- 按严重性报告问题
- 阻止严重问题继续
- 为较小问题提供修复建议

### 7. 完成分支 (Finishing a Branch)

完成所有工作后：

- 确保所有测试通过
- 提供合并、保持原样或丢弃的选项
- 清理临时工作区

## 代码风格指南

### TypeScript 风格

- 使用 TypeScript 严格模式
- 优先使用接口定义类型
- 使用 async/await 处理异步
- 添加适当的 JSDoc 注释

### 文件组织

```
src/
├── main.ts              # 插件主入口
├── api/                 # API 客户端
├── ui/                  # UI 组件
├── utils/               # 工具函数
└── types/               # 类型定义
```

### 命名约定

- 类名使用 PascalCase
- 函数和变量使用 camelCase
- 常量使用 UPPER_SNAKE_CASE
- 文件名使用 kebab-case

## Git 工作流

### 分支策略

- `main` - 生产就绪代码
- `feature/*` - 功能开发分支
- `fix/*` - 修复分支

### 提交信息格式

```
<type>(<scope>): <subject>

<body>
```

类型：
- `feat` - 新功能
- `fix` - 修复
- `docs` - 文档
- `style` - 格式
- `refactor` - 重构
- `test` - 测试
- `chore` - 构建/工具

## 验证清单

在标记任务完成前检查：

- [ ] 所有现有测试通过
- [ ] 添加了新功能的测试
- [ ] 代码遵循项目风格
- [ ] 文档已更新
- [ ] 没有遗留的调试代码

## 核心原则

1. **测试优先** - 测试驱动开发，先写测试
2. **简单性** - YAGNI，只实现需要的功能
3. **清晰性** - 代码要有良好的可读性
4. **验证** - 总是验证更改是否有效
5. **迭代** - 小步快跑，频繁验证
